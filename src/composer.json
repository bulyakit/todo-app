{
  "require": {
    "flynsarmy/slim-monolog": "^1.0",
    "guzzlehttp/guzzle": "^6.3",
    "psr/log": "^1.0",
    "slim/slim": "^3.0"
  },

  "autoload": {
    "psr-4": {
      "App\\": "./"
    }
  },

  "scripts": {
    "code-sniffer": "phpcs -p --standard=PSR12 src",
    "phpunit": "phpunit -c phpunit.xml",
    "refresh-database": "php ./sql/migrate.php",
    "rollback-database": "php ./sql/migrate.php -r"
  }
}
